name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest tests/ -v

      - name: Smoke test CLI
        run: |
          foliograph build examples/sample/sample.md \
            --output /tmp/fg_test/ \
            --name "CI Smoke Test" \
            --no-drift \
            --no-hooks
          test -f /tmp/fg_test/FOLIO_GRAPH.md
          test -f /tmp/fg_test/FOLIO_INDEX.md
          test -f /tmp/fg_test/FOLIO_SESSION.md
